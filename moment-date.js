moment(LoanDate).format(
            "Do MMMM, YYYY"
          )

moment(LoanDate).format(
            "DD/MM/YYYY"
          )
// invalid date solution
 {moment(date,"Do MMM, YYYY").format("Do MMM, YYYY")}
